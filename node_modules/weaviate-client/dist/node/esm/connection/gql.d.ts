import { Variables } from 'graphql-request';
import ConnectionREST, { ConnectionParams } from './http.js';
export default class ConnectionGQL extends ConnectionREST {
  private gql;
  constructor(params: ConnectionParams);
  query: <V extends Variables, T = any>(
    query: any,
    variables?: V | undefined
  ) => Promise<{
    data: T;
  }>;
  close: () => void;
}
export * from './auth.js';
export type TQuery = any;
export interface GraphQLClient {
  query: <V extends Variables, T>(
    query: TQuery,
    variables?: V,
    headers?: HeadersInit
  ) => Promise<{
    data: T;
  }>;
}
export declare const gqlClient: (config: ConnectionParams) => GraphQLClient;
